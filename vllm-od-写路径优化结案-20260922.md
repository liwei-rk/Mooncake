# vLLM + Mooncake + 盘框 写路径优化结案（2026-09-22）

> 单卡 A6000 (128) + vLLM od 版 + MooncakeDistributedStore + NDS 盘框（nsid=147815026, valueSize=1MB）
> 目标：优化 vLLM 单卡场景下 KV 存盘框的写路径延迟。不涉及 PD 分离 / SGLang / 跨节点。
> 代码改动：`mooncake-store/tests/od_kv_storage/mooncake_storage_interface.py`（kv_v8 分支，单文件 +70/-14）

---

## 1. 一句话结论

**大 prompt（3095 tok / 194 blocks）写路径 save 净开销从 655ms/请求降到 ~460ms（-30%）**，端到端写轮 1942→1793ms；途中发现并修复一个大 prompt 静默丢数据的正确性 bug（客户端 segment 打爆）；剩余 460ms 经实验证明主要是 D2H 物理传输 + CUDA 流反压（~300ms）与盘框写带宽（~100ms），软件层面已接近极限。

## 2. 基线（优化前，2026-09-22 实测）

| 场景 | HBM 命中 | 盘框读回 | 全新写轮（含 save） |
|---|---|---|---|
| 小 prompt 149tok / 9 blocks | 902ms | 917ms (+15) | 963ms (+62) |
| 大 prompt 3095tok / 194 blocks | 848ms（无写干扰） | 878ms (+30) | 1942ms |

- 读路径（盘框读回）本来就只 +15~30ms，不是瓶颈；**写路径是大头**。
- 注意大 prompt 的 HBM 基线必须在"无 save 队列干扰"下测（重启后读轮重发一次），否则数字虚高 ~90ms。
- 之前报告的"+1094ms 写开销"是测量错误：拿"全量 prefill + save"减"prefix 命中"，差值里混入了全量 prefill 的计算量。

## 3. 正确性 bug：客户端 segment 打爆 → 静默丢数据（重要）

### 现象

大 prompt 写轮后 master 计数：`PutStart:(Req=15/3/18, Item=795/862)` —— 3 个请求部分成功，67 个 item 丢失。master 日志：

```
W0922 15:35:50 rpc_service.cpp:560] BatchPutStart failed for 2 keys due to insufficient space.
W0922 15:35:51 ... failed for 4 keys ...
W0922 15:35:57 ... failed for 61 keys ...
```

### 根因

master 侧 `Mem Storage` 池 = **客户端 store 挂载的 segment**（`mount_segment segment_name=127.0.0.1:0`），不是 master 自己的内存。适配层 worker 侧 + scheduler 侧各挂 256MB（`Clients: 2`），而大 prompt 单请求 194 keys × 1MB = 194MB，第 2 个请求起 PutStart 直接失败，部分 block 没写到盘框。eviction（high_watermark 0.95）追赶不及。

### 修复代码

```python
rc = self._store.setup(
    local_hostname="127.0.0.1:0",
    metadata_server=self._meta_url,
    # WHY 2GB：master 侧 Mem Storage 池 = 客户端挂载的 segment。
    # 大 prompt（~3.6k tok）单请求 224 keys × 1MB = 224MB，
    # 256MB 只够 1 个请求缓冲，第 2 个请求起 PutStart 直接
    # insufficient space 失败（2026-09-22 实测 67/862 item 丢失）。
    # 2GB = 8 个大请求缓冲，eviction 有余量追赶。
    global_segment_size=2 * 1024 * 1024 * 1024,
    ...)
```

修复后写轮 PutStart/PutEnd 全成功。**这个 bug 对"g4 长文本落盘"场景是致命的（丢块导致前缀不命中），不是纯性能问题。**

## 4. 方法论：save 净开销怎么量出来的

1. **打点开关**：`MC_STORAGE_PROFILE=1`（已在 start_vllm_od.sh），适配层每次 xds_put 输出：

```
[mc-storage] PERF put segs=130 devsync=0.00ms d2h_loop=5.40ms flushsync=0.00ms putrpc=0.00ms (putrpc 含整 value=1024KB 落盘框)
```

2. **禁写对照**：新增环境变量 `MC_STORAGE_DISABLE_PUT=1`，xds_put 直接 no-op：

```python
# 禁写对照模式：记录成功位图即返回，不做任何 D2H/RPC
if self._disable_put:
    self._batch_results[req_id] = results
    return 0
```

跑全新 prompt 得到"全量 prefill + decode 不带 save"= **1334ms**，与正常写轮 1989ms 对比 → **save 净开销 = 655ms/请求**。优化后再测 = 460ms。

## 5. 两项已生效的优化

### 5.1 去掉 xds_put 的 cudaDeviceSynchronize（省 ~128ms/大请求）

vLLM 容器 worker.py 的 `_sync_and_submit_save` 本就有依赖链：

```python
with get_module(self._device_type).stream(self._store_stream):
    end_event.wait(self._store_stream)   # 该层 compute kernel → store_stream 排队的 D2H
    for io_metadata in self._store_input[layer_idx]:
        ret = self._storage_interface.xds_put(...)
```

依赖已细粒度保证，适配层里的 `cudaDeviceSynchronize()` 反而等了**后续层**的 compute kernel。删掉：

```python
# WHY 不再做 cudaDeviceSynchronize（2026-09-22 性能优化）：
# 依赖链已由 worker._sync_and_submit_save 的 end_event.wait(store_stream)
# 细粒度保证（该层 compute kernel → store_stream 上排队的 D2H），
# 全设备同步反而等待后续层 compute kernel——PERF 实测大 prompt
# (3095tok) devsync 累计 128ms/请求纯属浪费。若未来依赖链有变，
# 用 MC_STORAGE_VERIFY_D2H=1 复验 X==Y。
t0 = time.perf_counter()
t_devsync = (time.perf_counter() - t0) * 1e3   # 保留打点（值≈0）
```

### 5.2 逐 key stream_sync → flush 前统一一次（D2H 流水线化）

原代码每个 key 凑齐 28 层就 `cudaStreamSynchronize` 一次 → "提交 D2H→等完成→再提交"完全串行，无法流水线。PERF 实测大 prompt 累计 338ms 等待，而 194MB PCIe 传输本身只需 ~8ms。改为：

```python
# 凑齐全部层 → （批量路径）挂队列延迟统一 sync+提交
if len(slot.received_layers) >= self._total_layers:
    try:
        if self._dump_dir:
            self._stream_sync(stream_id)      # dump 调试模式保留原地 sync
        self._dump_value(...)
        if self._batch_put:
            self._flush_pending.append((key, slot))   # 只挂队列，不 sync
            ...
        else:
            self._stream_sync(stream_id)      # 逐 key 兜底路径：提交前必须等
            ...
```

```python
if self._batch_put and self._flush_pending:
    # 统一等 store_stream 上所有 D2H 完成（替代逐 key sync）
    ts0 = time.perf_counter()
    self._stream_sync(stream_id)
    t_flushsync = (time.perf_counter() - ts0) * 1e3   # 实测 0.01ms：D2H 早已完成
    t_putrpc += self._flush_pending_batch(value_size, results)
```

优化后 `flushsync≈0.01ms`——统一 sync 时所有 D2H 已在流上完成，等待完全消失。

## 6. 探索过但被证伪的方向（负结果，防止后人重走）

| 假设 | 实验 | 结果 |
|---|---|---|
| staging 是 pageable 内存导致 memcpyAsync 隐式阻塞 | `cudaHostRegister(staging, 600MB)` 注册 pinned（rc=0，pinned=True） | 写轮 1816.6 vs 1821ms，**无差异** |
| 匿名 mmap 首次触碰缺页税（1MB=256页×20μs≈5ms） | init 时 `self._staging_view[:] = 0` 预触碰全部 600MB | 写轮 1794 vs 1817ms，**无差异**；slow 段从 i=0 换到 i=111/253 |
| devsync 是 save 慢的主因 | 删掉 cudaDeviceSynchronize | **有效**，-128ms（已采纳） |

两个负结果代码保留在仓库（pinned 注册 + 预触碰各 3-5 行，带 WHY 注释），无害且留作证据。

## 7. 剩余 ~460ms 的构成（全部有实测证据）

| 构成 | 耗时/请求 | 证据 | 可优化性 |
|---|---|---|---|
| D2H 传输 + CUDA 流反压 | ~300ms | 细分打点 `pre=0.00ms mc=6-7.4ms`，**固定卡 i=253**（260 段调用）= 流队列深度反压；copy engine 与 compute 争显存带宽 | 物理，软件难压（pinned/预触碰已证伪） |
| 盘框 put RPC | ~100ms | 2 批 × 97 keys × 1MB ≈ 50ms/批 = 1.9GB/s NDS 单批写带宽 | 需 4MB nsid 聚合（减 key 数）或确认 NDS 带宽极限 |
| Python 调用开销 | ~60ms | 280 次调用 × 未打点部分 | 小，不值得动 |

## 8. 最终 A/B 结果

### 大 prompt（3095 tok / 194 blocks）

| 指标 | 优化前 | 优化后 |
|---|---|---|
| 全新写轮 | 1942-1989ms | **1793ms (-8~10%)** |
| save 净开销 | 655ms | **~460ms (-30%)** |
| 盘框读回 | 878ms | 868ms（不变，本来就好） |
| 数据正确性 | — | PutEnd 5131/5131、Get 11379/11379 全绿，读轮输出正常 |

### 小 prompt（149 tok / 9 blocks）回归

| 场景 | 优化前 | 优化后 |
|---|---|---|
| HBM 命中 | 902ms | 903ms（持平） |
| 盘框读回 | 917ms | 920ms（持平） |
| 全新写轮 | 963ms | 978ms（±噪声） |

小 prompt 的 save 开销本来就只有 ~62ms，本轮优化目标是大 prompt 场景，小场景无退化。

## 9. 新增工具与环境变量索引

| 开关 | 功能 | 用法 |
|---|---|---|
| `MC_STORAGE_PROFILE=1` | PERF put/exist/get 打点（新增 flushsync 字段与 seg slow 细分） | start_vllm_od.sh 已开启 |
| `MC_STORAGE_DISABLE_PUT=1` | xds_put no-op，测无 save 纯计算基线 | 临时加进 start_vllm_od.sh，用完删 |
| `MC_STORAGE_VERIFY_D2H=1` | D2H 数据时机复验（X==Y），依赖链改动后的正确性回归 | 已有开关，本轮复验通过 |
| 段级打点 | `PERF seg slow i=.. pre=..ms mc=..ms`（>5ms 才输出） | PROFILE=1 自动生效 |

## 10. 相关文件

| 位置 | 内容 |
|---|---|
| kv_v8 `mooncake-store/tests/od_kv_storage/mooncake_storage_interface.py` | 本轮全部代码改动（三处同步、md5 `c3cf2c27`/后续版本三处一致） |
| 服务器 `/home/yyc/start_vllm_od.sh` | 新增 `MC_STORAGE_PROFILE=1` |
| 本地 `pf1~pf26_*.py` | 本轮全部测量/部署/诊断脚本（paramiko SSH 驱动，可复跑） |
| 本地 `perf_send.py` / `perf_send_long.py`（容器 /tmp/） | 小/大 prompt 三场景压测脚本 |
| `/tmp/restart_vllm_only.sh`（服务器） | 只重启 vLLM 保 master；注意日志落在 `/tmp/vllm_fix_read.log` 不是 vllm_0922.log |

## 11. 下一步候选（未做）

1. **4MB nsid 聚合**（mooncake_03 = 2761673214）：key 数减 4 倍，put RPC / master 元数据 / 盘框 key 空间占用全降。需改适配层 value 布局（4 block/4MB）+ master `--nsid=2761673214` 重启。预估再省 50-80ms
2. **多并发场景摸底**：单并发已接近物理极限；g4 真实负载若为多请求并发，D2H 争抢 / RPC 排队 / segment 2GB 的多请求缓冲是否够用都值得测
3. **pynds 写带宽基准**：确认 1.9GB/s 是否为 NDS 1MB-value 写的极限（历史记录 32MB 大 value 多线程可到 8GB/s）
